repositories {
    jcenter() // jenkins (required by org.kohsuke.github-api) doesn't live on mavenCentral
}

dependencies {
    compile project(':cicomponents-git-api')
    compile 'org.kohsuke:github-api:1.76'
}